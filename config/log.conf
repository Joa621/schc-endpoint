[global]
strict init = true
buffer min = 1024
buffer max = 2MB
rotate lock file = /tmp/zlog.lock
default format = "%d.%ms %-6V %m%n"

[formats]
simple = "%m%n"

[rules]
ok.DEBUG    >stderr; simple
error.DEBUG >stderr; simple
rx.DEBUG    >stderr; simple
